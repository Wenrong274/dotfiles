# vscode-neovim 設定驗證清單

> 操作流程：`Ctrl+Shift+P` → "Reload Window" → 開一份 C# 檔，照下表逐項驗證
> 出問題的項目打 ❌，正常的打 ✅，回來告訴我哪些壞掉
>
> **前置需求**：第 38–44 項（LSP / Leader 功能）需要 C# 擴充套件已載入且 LSP 正常運作，
> 若 LSP 尚未啟動，這幾項跳過即可。

---

## 🔴 整檔操作驗證

| #   | 測試動作             | 預期結果                      | 結果 |
| --- | -------------------- | ----------------------------- | :--: |
| 1   | Normal mode 按 `yae` | 整個檔案被複製到 register     |      |
| 2   | 在空白處按 `p`       | 貼出整個檔案內容              |      |
| 3   | Normal mode 按 `vae` | 整個檔案被選取（visual mode） |      |
| 4   | Normal mode 按 `dae` | 整個檔案被刪除（可 `u` undo） |      |

---

## 🟠 搜尋 / 縮排驗證

| #   | 測試動作                        | 預期結果                    | 結果 |
| --- | ------------------------------- | --------------------------- | :--: |
| 5   | `/foo` 搜尋後，按 `<Space><CR>` | highlight 消失              |      |
| 6   | Visual 選 3 行，按 `>`          | 縮排 +1，**仍保留選取**     |      |
| 7   | 接續上面按 `>` 再一次           | 再縮排 +1，**還是選取狀態** |      |
| 8   | Visual 選 3 行，按 `<`          | outdent，仍保留選取         |      |

---

## 🟢 Insert Mode 跳脫

| #   | 測試動作                                   | 預期結果                           | 結果 |
| --- | ------------------------------------------ | ---------------------------------- | :--: |
| 9   | 按 `i` 進 Insert，快速按 `jj`              | 回到 Normal（游標變方塊）          |      |
| 10  | 按 `i` 進 Insert，快速按 `jk`              | 回到 Normal                        |      |
| 11  | Insert mode 慢慢打 `j`（等 300ms）再打 `j` | 留下兩個 `j` 字元（不觸發 escape） |      |

---

## 🟢 視窗切換

| #   | 測試動作                  | 預期結果         | 結果 |
| --- | ------------------------- | ---------------- | :--: |
| 12  | Normal mode 按 `<Space>v` | 編輯器垂直分割   |      |
| 13  | `<C-l>`                   | 焦點移到右邊群組 |      |
| 14  | `<C-h>`                   | 焦點移回左邊     |      |
| 15  | `<Space>s`                | 水平分割         |      |
| 16  | `<C-j>` / `<C-k>`         | 上下群組焦點切換 |      |

---

## 🟢 Tab 切換

| #   | 測試動作                 | 預期結果       | 結果 |
| --- | ------------------------ | -------------- | :--: |
| 17  | 開兩個 Tab，按 `Shift+L` | 切到下一個 Tab |      |
| 18  | `Shift+H`                | 切到上一個 Tab |      |
| 19  | `<Space>w`               | 關閉當前 Tab   |      |

---

## 🟢 搜尋 / 符號

| #   | 測試動作   | 預期結果                    | 結果 |
| --- | ---------- | --------------------------- | :--: |
| 20  | `<Space>/` | 開啟 Find in Files 全域搜尋 |      |
| 21  | `<Space>g` | 開啟當前檔案符號跳轉清單    |      |
| 22  | `<Space>G` | 開啟 Workspace 符號搜尋     |      |

---

## 🟡 Jumplist

| #   | 測試動作                        | 預期結果           | 結果 |
| --- | ------------------------------- | ------------------ | :--: |
| 23  | 在 A 點 → `gd` 跳定義 → `<C-o>` | 跳回 A 點          |      |
| 24  | 接續 `<C-i>`                    | 跳回定義處（前進） |      |

> vscode-neovim 使用 VSCode 的 jumplist，所以 `gd` 跳定義也會記錄在跳轉歷史中。

---

## 🟡 VSCode 快捷鍵（Insert mode）

| #   | 測試動作                | 預期結果             | 結果 |
| --- | ----------------------- | -------------------- | :--: |
| 25  | Insert mode 按 `Ctrl+S` | 存檔                 |      |
| 26  | Insert mode 按 `Ctrl+A` | 全選                 |      |
| 27  | Insert mode 按 `Ctrl+F` | 開啟 VSCode 檔內搜尋 |      |
| 28  | Insert mode 按 `Ctrl+Z` | Undo                 |      |

> 在 vscode-neovim 中，Insert mode 由 VSCode 原生處理，Ctrl 系列自然可用。

---

## 🔵 Flash.nvim

| #   | 測試動作                        | 預期結果                 | 結果 |
| --- | ------------------------------- | ------------------------ | :--: |
| 29  | Normal mode 按 `<Space><Space>` | 進入 Flash 模式          |      |
| 30  | 接續打 1~2 個字元               | 畫面上匹配處出現標記字母 |      |
| 31  | 按標記字母                      | 跳到該位置               |      |

---

## 🔵 Surround（nvim-surround）

| #   | 測試動作                   | 預期結果          | 結果 |
| --- | -------------------------- | ----------------- | :--: |
| 32  | 游標在 word 上，按 `ysiw"` | word 被 `""` 包覆 |      |
| 33  | 按 `cs"'`                  | `""` 變成 `''`    |      |
| 34  | 按 `ds'`                   | 引號被刪除        |      |

---

## 🔵 CamelCaseMotion

| #   | 測試動作                              | 預期結果 | 結果 |
| --- | ------------------------------------- | -------- | :--: |
| 35  | 游標在 `MyVariableName` 開頭，按 `,w` | 跳到 `V` |      |
| 36  | 再按 `,w`                             | 跳到 `N` |      |
| 37  | 按 `,b`                               | 跳回 `V` |      |

---

## 🔵 LSP / Leader 功能

| #   | 測試動作                  | 預期結果             | 結果 |
| --- | ------------------------- | -------------------- | :--: |
| 38  | 游標放 method 名上按 `gd` | 跳到定義             |      |
| 39  | `gr`                      | 開啟 references 面板 |      |
| 40  | `<Space>r`                | 觸發 Rename          |      |
| 41  | `<Space>a`                | 開啟 Quick Fix 燈泡  |      |
| 42  | `<Space>c`                | Format Document      |      |
| 43  | `<Space>b`                | Toggle breakpoint    |      |
| 44  | `gcc`                     | 註解當前行           |      |

---

## 🔵 系統剪貼簿

| #   | 測試動作                                   | 預期結果                                 | 結果 |
| --- | ------------------------------------------ | ---------------------------------------- | :--: |
| 45  | `yy` 複製一行 → 切到瀏覽器 → `Ctrl+V`      | 貼出該行內容                             |      |
| 46  | 在瀏覽器 `Ctrl+C` 一段文字 → 回 VSCode `p` | 貼出該文字                               |      |
| 47  | `"_dd` 刪一行 → `Ctrl+V` 到外面            | 外面**不會**被覆蓋（黑洞 register 生效） |      |

---

## 🔵 IME 自動切換

| #   | 測試動作                                      | 預期結果         | 結果 |
| --- | --------------------------------------------- | ---------------- | :--: |
| 48  | Insert mode 切到中文打幾個字 → `jj` 出 Normal | IME 自動切回英文 |      |
| 49  | 再按 `i` 進 Insert                            | IME 自動還原中文 |      |

---

## 🔵 Unity 整合

| #   | 測試動作   | 預期結果                   | 結果 |
| --- | ---------- | -------------------------- | :--: |
| 50  | `<Space>U` | 觸發 Attach Unity Debugger |      |

---

## 🔵 mini.ai 引數文字物件

> 找一段函式呼叫測，例如 `DoSomething(playerId, weaponName, "fireball");`，游標放 `weaponName` 上。

| #   | 測試動作 | 預期結果                                                | 結果 |
| --- | -------- | ------------------------------------------------------- | :--: |
| 51  | `dia`    | 變成 `DoSomething(playerId, , "fireball");`（只刪本體） |      |
| 52  | `daa`    | 變成 `DoSomething(playerId, "fireball");`（含逗號）     |      |
| 53  | `cia`    | 進 Insert，引數本體被清空                               |      |
| 54  | `via`    | Visual 選取 `weaponName`                                |      |

---

## 🔵 yank 高亮

| #   | 測試動作                                | 預期結果                                | 結果 |
| --- | --------------------------------------- | --------------------------------------- | :--: |
| 55  | 在 terminal 開 `nvim init.lua`，按 `yy` | 該行短暫泛黃 200ms                      |      |
| 56  | 在 VSCode（vscode-neovim）內按 `yy`     | 高亮可能不顯示（已知限制），yank 仍正常 |      |

---

## 🔵 Splitjoin / Align

> Splitjoin 可用 JSON / HTML / Lua 測；mini.align 用多行 `=` 最直觀。

| #   | 測試動作                                                           | 預期結果        | 結果 |
| --- | ------------------------------------------------------------------ | --------------- | :--: |
| 57  | 游標放在單行 JSON 或 tag 上，按 `gS`                               | 結構被拆成多行  |      |
| 58  | 游標放回該 block 第一行，按 `gJ`                                   | 結構合回單行    |      |
| 59  | Visual 選 3 行 `foo = 1` / `long_name = 22` / `x = 333` 後按 `ga=` | `=` 欄位對齊    |      |
| 60  | 接續另一組逗號分隔欄位，Visual 後按 `ga,`                          | 依 `,` 對齊欄位 |      |

---

## 完成後

記下失敗的項目編號，對照 `vscode-nvim/docs/vim-guide.md` 相關章節重新排查設定。

60 項全 ✅ — vscode-neovim 設定為穩定可用狀態。

---

最後更新：2026-05-20
